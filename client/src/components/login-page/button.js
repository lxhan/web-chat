import styled from 'styled-components';

const Button = styled.button`
    padding: 0.6em 1em;
    background: #2e305a;
    border: none;
    border-radius: 3px;
    color: #fff;
    margin-top: 1em;
`;

export default Button;